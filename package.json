{
  "name": "marketplace",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "http-server . -p 4173 -a 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:cf": "npm run build && wrangler pages deploy dist --project-name marketplace"
  },
  "dependencies": {
    "devextreme": "25.1.7"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "vite": "^5.3.0",
    "wrangler": "^4.46.0"
  }
}
